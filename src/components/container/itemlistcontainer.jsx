import React from 'react';

const ItemListContainer = ({greeting}) => {
    return (
        <p style={{color: "white", backgroundColor:"black"}}>{greeting}</p>
    );
}

export default ItemListContainer;